describe('data types in typescript', () => {
  describe('literal types', () => {
    it('basic test', () => {
      let a = 10;
      let b = 20;

      let answer = a + b;

      expect(answer).toBe(30);
    });
  });
});
